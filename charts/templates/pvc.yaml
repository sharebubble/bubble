{{- if .Values.backend.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "bubble.fullname" . }}-data
  labels:
    {{- include "bubble.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.backend.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.backend.persistence.size }}
  {{- if or .Values.backend.persistence.storageClass .Values.global.storageClass }}
  storageClassName: {{ .Values.backend.persistence.storageClass | default .Values.global.storageClass }}
  {{- end }}
{{- end }}
---
{{- if .Values.postgresql.enabled }}
{{- if .Values.postgresql.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "bubble.postgresql.fullname" . }}-data
  labels:
    {{- include "bubble.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.postgresql.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.postgresql.persistence.size }}
  {{- if or .Values.postgresql.persistence.storageClass .Values.global.storageClass }}
  storageClassName: {{ .Values.postgresql.persistence.storageClass | default .Values.global.storageClass }}
  {{- end }}
{{- end }}
{{- end }}
