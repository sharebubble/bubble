apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bubble.fullname" . }}-backend
  labels:
    {{- include "bubble.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.backend.django }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  REDIS_URL: {{ include "bubble.redis.url" . | quote }}
